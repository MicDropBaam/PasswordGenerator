# Password Generator

A simple Python script to generate a strong, random password of a desired length.

## Features
- Supports uppercase, lowercase, digits, and special characters
- Fully random every time
- Easy to run from terminal

## How to Run
```bash
python password_generator.py
